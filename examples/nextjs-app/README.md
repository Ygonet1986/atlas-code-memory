# Example: Next.js-style app layout (files only)

Suggest scopes: `app/`, `lib/`, `components/`.

```bash
atlas onboard -C examples/nextjs-app
atlas graph add app --escopo app
atlas graph add lib --escopo lib
```
