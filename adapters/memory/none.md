# MemoryBackend: none

Atlas Core still provides:

- `mempalace-index` (can list intended rooms even without a palace)
- `graphify-index` + optional graph tool
- `project-cache` (required)

Use this when you only want file/graph routing without long-term chat memory.
